#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	/* Declare variable and assign output string to it */
	char myChar[] = "with proper grammar, but the outcome is a piece of art,";

	/* Output string to screen (stdout) */
	printf("%s\n", myChar);

	/* return 0 to end program */
	return (0);
}
