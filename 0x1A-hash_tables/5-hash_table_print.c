#include "hash_tables.h"

/**
 * hash_table_print - Prints all nodes in the hash table.
 *
 * @ht: Pointer to hash table for printing.
 */

void hash_table_print(const hash_table_t *ht)
{
	if (!ht || !(void *)ht)
		ht = NULL;
}
