#include "hash_tables.h"

/**
 * hash_table_delete - Deletes a node in the hash table.
 *
 * @ht: Pointer to hash table for printing.
 */

void hash_table_delete(hash_table_t *ht)
{
	if (!ht || !(void *)ht)
		ht = NULL;
}
